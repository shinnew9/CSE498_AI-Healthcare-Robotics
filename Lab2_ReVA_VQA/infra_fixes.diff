diff --git a/qwen_finetune/qwenvl/train/train_qwen.py b/qwen_finetune/qwenvl/train/train_qwen.py
index f67d569..f3fb2f2 100644
--- a/qwen_finetune/qwenvl/train/train_qwen.py
+++ b/qwen_finetune/qwenvl/train/train_qwen.py
@@ -203,4 +203,4 @@ def train(attn_implementation="flash_attention_2"):
 
 
 if __name__ == "__main__":
-    train(attn_implementation="flash_attention_2")
+    train(attn_implementation="sdpa")
diff --git a/qwen_finetune/qwenvl/train/trainer.py b/qwen_finetune/qwenvl/train/trainer.py
index a5fe3ea..f9e9888 100644
--- a/qwen_finetune/qwenvl/train/trainer.py
+++ b/qwen_finetune/qwenvl/train/trainer.py
@@ -1,7 +1,10 @@
 from typing import Dict, List, Optional, Sequence, Tuple, Callable
 
 import torch
-from flash_attn.flash_attn_interface import flash_attn_varlen_func
+try:
+    from flash_attn.flash_attn_interface import flash_attn_varlen_func
+except ImportError:
+    flash_attn_varlen_func = None
 from transformers.modeling_flash_attention_utils import FlashAttentionKwargs
 from transformers import Trainer
 from transformers.cache_utils import Cache
@@ -10,8 +13,11 @@ from transformers.processing_utils import Unpack
 from transformers.models.qwen2_vl.modeling_qwen2_vl import (
     Qwen2VisionTransformerPretrainedModel,
     Qwen2VLModel,
-    apply_multimodal_rotary_pos_emb,
 )
+try:
+    from transformers.models.qwen2_vl.modeling_qwen2_vl import apply_multimodal_rotary_pos_emb
+except ImportError:
+    apply_multimodal_rotary_pos_emb = None
 from transformers.models.qwen2_5_vl.modeling_qwen2_5_vl import (
     Qwen2_5_VisionTransformerPretrainedModel,
     Qwen2_5_VLModel,
diff --git a/qwen_finetune/scripts/sft_7b.sh b/qwen_finetune/scripts/sft_7b.sh
index bce0eaa..bb0c1f6 100755
--- a/qwen_finetune/scripts/sft_7b.sh
+++ b/qwen_finetune/scripts/sft_7b.sh
@@ -9,7 +9,7 @@ MASTER_PORT=${MASTER_PORT:-$(python3 -c 'import random; print(random.randint(200
 NPROC_PER_NODE=${NPROC_PER_NODE:-1}
 
 # DeepSpeed configuration
-deepspeed=./scripts/zero3.json
+deepspeed=${DEEPSPEED_CONFIG:-./scripts/zero3.json}
 use_deepspeed=${USE_DEEPSPEED:-1}
 
 # Model configuration
@@ -45,7 +45,7 @@ report_to=${REPORT_TO:-"none"}
 args="
     --model_name_or_path "${llm}" \
     --dataset_use ${datasets} \
-    --data_flatten True \
+    --data_flatten False \
     --tune_mm_vision False \
     --tune_mm_mlp True \
     --tune_mm_llm True \
@@ -72,7 +72,6 @@ args="
     --save_total_limit 1 \
     --learning_rate ${lr} \
     --weight_decay 0 \
-    --warmup_ratio 0.03 \
     --max_grad_norm 1 \
     --lr_scheduler_type "cosine" \
     --logging_steps 1 \
diff --git a/reva_eval/eval_reva_v2.sh b/reva_eval/eval_reva_v2.sh
index 04c82d7..5f5f0d3 100755
--- a/reva_eval/eval_reva_v2.sh
+++ b/reva_eval/eval_reva_v2.sh
@@ -108,7 +108,7 @@ for idx in $(seq 0 $((NUM_CHUNKS - 1))); do
     if [ -n "$MODEL_BASE" ]; then
         extra_args+=(--model-base "$MODEL_BASE")
     fi
-    CUDA_VISIBLE_DEVICES=$idx conda run -n "$CONDA_ENV" python3 inference_vllm_origin_number.py \
+    CUDA_VISIBLE_DEVICES=${EVAL_GPU:-$idx} conda run -n "$CONDA_ENV" python3 inference_vllm_origin_number.py \
         --model-path "$MODEL_PATH" \
         "${extra_args[@]}" \
         --video_dir "$REVA_ROOT" \
diff --git a/reva_eval/inference_vllm_origin_number.py b/reva_eval/inference_vllm_origin_number.py
index f01fbe0..bb1ba57 100644
--- a/reva_eval/inference_vllm_origin_number.py
+++ b/reva_eval/inference_vllm_origin_number.py
@@ -33,7 +33,7 @@ _vvu_spec.loader.exec_module(_vvu_mod)
 process_vision_info = _vvu_mod.process_vision_info
 cached_process_vision_info = _vvu_mod.cached_process_vision_info
 
-from transformers import AutoModelForCausalLM, AutoTokenizer, AutoProcessor, GenerationConfig, AutoConfig, AutoModelForVision2Seq
+from transformers import AutoModelForCausalLM, AutoTokenizer, AutoProcessor, GenerationConfig, AutoConfig, AutoModelForImageTextToText
 from transformers import Qwen2_5_VLForConditionalGeneration, Qwen2_5_VLConfig, Qwen3VLForConditionalGeneration
 from collections import defaultdict
 
@@ -268,7 +268,7 @@ def normalize_fps_for_processor(processor, fps):
 def load_transformers_vl_model(model_path: str, **kwargs):
     if is_qwen3_model(model_path):
         return Qwen3VLForConditionalGeneration.from_pretrained(model_path, **kwargs)
-    return AutoModelForVision2Seq.from_pretrained(model_path, use_sliding_window=True, **kwargs)
+    return AutoModelForImageTextToText.from_pretrained(model_path, use_sliding_window=True, **kwargs)
 
 
 def run_inference(args):
@@ -371,7 +371,7 @@ def run_inference(args):
     args.answers_file = answers_file
 
     dataset = VideoQADataset(args, processor)
-    batch_size = 8 // args.repeat_times
+    batch_size = int(os.environ.get("EVAL_BATCH_SIZE", 8)) // args.repeat_times
     dataloader = DataLoader(
         dataset,
         batch_size=batch_size,
@@ -503,6 +503,12 @@ def run_inference(args):
                 ans_file.write(json.dumps(sample_set) + "\n")
                 # ranki_print(f"input={sample['llm_inputs']['prompt']}, output={pred_text}")
         ans_file.flush()
+        if args.backend != 'vllm':
+            # Free per-iteration GPU tensors; without this, allocated memory grows
+            # until a 11GB card OOMs partway through a long evaluation run.
+            del input_ids, attention_mask, pixel_values_videos, video_grid_thw
+            del generated_ids, generated_ids_trimmed, inputs
+            torch.cuda.empty_cache()
     ans_file.close()
 
 
